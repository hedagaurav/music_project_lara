Basic Task : -
<p>1. Create Login and Logout.</p>
<p>2. Create Userprofile upload (min and max pixel allowed 200 only).</p>

Main Task : -
we have 3 tables (client_data,metadata_data, metadata_status)
<p>1. create search modal Label Name, song_name, search by date, search by status.</p>
<p>2. add one button for export data based on search condition data export if no search condition runs and click on export data all data will get exported.</p>

Note : 
<ol>
    
    <li>metadata_data -> musicLabel = client_data -> id</li>
    <li>metadata_data -> status = metadata_status-> status_id</li>
    <li>search by date = metadata_data -> createdOn </li>
    <li>Export Data output -> excel file .xlsx format (no .csv file)</li>
    <li>export data : metadata_data all -> replace (musicLabel number to client_data(parentLabelCompany)) and replace(status -> metadata_status(status))</li>
    <li>metadata_data -> inlay_file_url images you can find in zip file set all images properly.</li>
    <li>database demo file and the design part also you can find in the zip file.</li>
</ol>